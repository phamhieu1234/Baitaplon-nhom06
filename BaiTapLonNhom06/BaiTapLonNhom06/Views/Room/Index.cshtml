@model IEnumerable<BaiTapLonNhom06.Models.Room>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GiaPhong)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CSVC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GiaPhong.MaGiaPhong)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CSVC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RommID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RommID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RommID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
