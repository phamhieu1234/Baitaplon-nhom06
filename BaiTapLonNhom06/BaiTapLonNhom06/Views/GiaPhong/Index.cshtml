@model IEnumerable<BaiTapLonNhom06.Models.GiaPhong>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenGiaPhong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenGiaPhong)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaGiaPhong">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaGiaPhong">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaGiaPhong">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
